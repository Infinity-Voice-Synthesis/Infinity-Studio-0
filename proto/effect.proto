syntax = "proto3";

package org.infinity.idm;

import "tparam.proto";

message Effector {
    string name = 1;//效果器名称
    bool isOn = 2;//开启
    double mix = 3;//混合系数

    repeated org.infinity.idm.TrackParam params = 4;//参数
}
